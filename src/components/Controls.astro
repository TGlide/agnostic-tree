<div class="p-12">
  <h3 class="text-2xl font-bold">Controls</h3>
  <h4 class="font-semibold mbs-8">When focusing an item or group</h4>
  <ul class="text-sm leading-snug list-disc pis-14 mbs-4">
    <li>
      <kbd>Enter</kbd> to select the currently focused entity.
    </li>
    <li>
      <kbd>↑</kbd> to focus the previous entity.
    </li>
    <li>
      <kbd>↓</kbd> to focus the next entity.
    </li>
    <li>
      <kbd>←</kbd> to focus the parent group, if any.
    </li>
    <li>
      <kbd>a-z</kbd> will select the first visible entity that starts with the
      pressed key. If the current entity starts with the pressed key, it will
      select the next entity that starts with the pressed key.
    </li>
  </ul>
  <h4 class="font-semibold mbs-8">When focusing a group</h4>
  <ul class="text-sm leading-snug list-disc pis-14 mbs-4">
    <li>
      <kbd>←</kbd> to collapse it. If collapsed, it'll focus the parent group.
    </li>
    <li>
      <kbd>→</kbd> to expand it. If expanded, it'll focus the first child.
    </li>
  </ul>
</div>

<style>
  kbd {
    display: inline-block;
    padding: 0.125rem 0.375rem;
    color: var(--clr-gray-10);
    white-space: nowrap;
    margin-block: 0.25rem;
    background-color: var(--clr-gray-3);
    border: solid 1px var(--clr-gray-7);
    border-radius: 0.25rem;
    box-shadow: 0 2px var(--clr-gray-7);
    font-size: 0.75rem;
  }
</style>
